services:
  fiber_gorm:
    build:
      context: .
    volumes:
      - ./:/app
    tty: true
    ports:
      - 9093:9093
    networks:
      - golang_basic_training

networks:
  golang_basic_training:
    driver: bridge
